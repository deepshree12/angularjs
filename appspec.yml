version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 12
    commands:
      - echo "installing dependencies..."
      - npm install
      - npm install -g @angular/cli
  build:
    commands:
      # - echo testing...
      # - echo building...
      - ng build --prod
  post_build:
    commands:
      - echo 
